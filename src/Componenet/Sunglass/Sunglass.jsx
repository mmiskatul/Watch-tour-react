import React from 'react'
import './Sunglass.css'
import { add, multiply } from '../../Utiles/Calculate';
// import {add} from '../../Utiles/Calculate';
function Sunglass() {
  const first =55;
  const second =199;
  const sum=add(first,second);
  const multi=multiply(first,second);

  return (
    <div>
      
    </div>
  )
}

export default Sunglass
